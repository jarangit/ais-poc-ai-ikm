import React from "react";
import ChatPage from "../components/chat";

type Props = {};

const Page = (props: Props) => {
  return (
    <div>
      <ChatPage />
    </div>
  );
};

export default Page;
